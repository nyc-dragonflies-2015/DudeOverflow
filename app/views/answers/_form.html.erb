<%= form_for @answer, :html => {id: 'answer-form'} do |f| %>
  <%= f.text_field :body %>
  <%= f.hidden_field :user_id, value: current_user.id %>
  <%= f.hidden_field :question_id, value: @question.id %>
  <%= f.submit 'Add Answer', class: "btn" %>
<% end %>
